# Compute per-peptide power (helper for plot_power_vs_effect)

Compute per-peptide power (helper for plot_power_vs_effect)

## Usage

``` r
compute_perpeptide_power(
  fits_data,
  n_per_group,
  effect_size,
  alpha,
  test,
  n_sim
)
```
